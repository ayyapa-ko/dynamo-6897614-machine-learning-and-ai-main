You are tasked with debugging a synthetic Machine Learning serving repository located at `/app/project`. The system implements high-throughput inference serving via a dynamic batching scheduler and a speculative decoding router. Currently, the production simulation fails both performance thresholds and operational constraints due to two interacting defects.

### Task Goal
Identify and resolve both defects so that the deterministic replay benchmark fulfills all performance and architectural metrics.

### System Components & Architecture
- `/app/project/config.yaml`: Serving parameters (`max_batch_delay`, `max_batch_size`, `speculative_threshold`). One parameter is misconfigured.
- `/app/project/scheduler.py`: Groups incoming requests into batches, respecting batch delays and tracking starvation occurrences.
- `/app/project/router.py`: Routes requests between the speculative decoding track and the standard autoregressive track based on prompt details and capacity. Contains a programmatic bug.
- `/app/project/benchmark.py`: Runs a deterministic replay trace, calculates throughput, p99 latency, tracks KV-cache health, and validates logs.

### Requirements
1. Modify `/app/project/config.yaml` and/or `/app/project/router.py` to fix the system degradation.
2. Running the benchmark script via `python3 /app/project/benchmark.py` must complete with zero errors and satisfy the following targets:
   - **Throughput**: $\ge 50$ requests per second.
   - **p99 Latency**: $\le 150$ milliseconds.
   - **Starvation Events**: Exactly 0 `SCHEDULER_STARVATION` events recorded in logs.
   - **KV-Cache Bounds**: No cache overflows or structural allocation failures.
   - **Functional Integrity**: The textual outputs generated by both tracks must match the expected trace baseline outputs exactly.

Do not alter `/app/project/benchmark.py`, `/app/project/replay.py`, or the simulation harness parameters. Your updates must be saved directly to the target configuration and repository source files.